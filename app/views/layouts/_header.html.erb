<nav class="navbar navbar-expand-lg navbar-light bg-light p-2 justify-content-between">
	<% if user_signed_in? %>
  		<a class="navbar-brand" href="/" ><%= current_user.name %></a>
  	<% else %>
  		<a class="navbar-brand" href="/">Hey Good Morning!</a>
  	<% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="d-flex flex-end">
  	<% if user_signed_in? %>
  		<%= link_to "Log Out", destroy_user_session_path, class: 'btn btn-success', style: 'color: white' %>
  	<% else %>
  		<%= link_to "Log In", new_user_session_path, class: 'btn btn-success', style: 'color: white' %>
  	<% end %>
  </div>
</nav>